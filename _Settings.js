// Settings for TaCZ: Zombies

export const PointSettings = {
    ZombieAmount: 100,     // Int
    BabyZombieAmount: 125, // Int
    BruteAmount: 500,      // Int
}

export const PlayerSettings = {
    Speed = 0.9,              // Float (defult = 1)
    JumpHeight = 0.35,        // Float 
    DividePointsAmount = 2,   // Float (change to 1 for no changes)

    SpectatorOnDeath = true, // Bool
}